执行用时：4 ms, 在所有 C++ 提交中击败了99.74%的用户
内存消耗：13.4 MB, 在所有 C++ 提交中击败了81.43%的用户
思路：双指针
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if(nums.empty())
            return 0;
        int i=0,j=1;
        for(;j<nums.size();j++){
            if(nums[i]!=nums[j])
                nums[++i]=nums[j];
        }
        return i+1;
    }
};
